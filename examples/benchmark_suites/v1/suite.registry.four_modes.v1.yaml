# Four registry-backed task shapes: repo governance, Markdown synthesis, matrix reasoning,
# browser-evidence interpretation (fixture path embedded in the prompt body).
schema_version: 1
id: bench.examples.suite.registry.four_modes.v1
title: Registry four task modes (repo, markdown, matrix, browser fixture)
rubric_ref: examples/dataset/rubrics/comparison.v1.json
prompts:
  - id: p-repo
    prompt_ref: bench.task.repo_governed.v1
  - id: p-md
    prompt_ref: bench.task.markdown_synthesis.v1
  - id: p-matrix
    prompt_ref: bench.task.matrix_reasoning.v1
  - id: p-browser
    prompt_ref: bench.task.browser_evidence.v1
variants:
  - id: v-cli
    agent_stack: alwm-cli
    execution_mode: cli
    backend:
      kind: mock
      model: mock-model
