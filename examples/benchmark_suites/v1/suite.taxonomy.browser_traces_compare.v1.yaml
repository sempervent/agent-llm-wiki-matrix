# Taxonomy: dual browser_mock variants (checkout_flow vs form_validation) for richer report smoke.
schema_version: 1
id: bench.examples.taxonomy.browser_traces_compare.v1
title: Taxonomy — browser traces compare (two fixtures)
rubric_ref: examples/dataset/rubrics/browser_realism.v1.json
taxonomy:
  task_family: browser_evidence
  difficulty: medium
  determinism: deterministic_fixture
  tool_requirements: [browser_mock, registry, cli]
tags: [taxonomy, browser, evidence, dual_fixture]
expected_artifact_kinds:
  - benchmark_request
  - benchmark_response
  - evaluation
  - browser_evidence
prompts:
  - id: p-one
    prompt_ref: bench.task.browser_evidence.v1
variants:
  - id: v-checkout
    agent_stack: browser-mock
    execution_mode: browser_mock
    backend:
      kind: mock
      model: mock-model
    browser:
      runner: file
      fixture_relpath: fixtures/browser_evidence/v1/checkout_flow.json
  - id: v-form
    agent_stack: browser-mock
    execution_mode: browser_mock
    backend:
      kind: mock
      model: mock-model
    browser:
      runner: file
      fixture_relpath: fixtures/browser_evidence/v1/form_validation.json
