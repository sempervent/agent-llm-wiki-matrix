Browser-evidence interpretation (fixture-only; no live browser). Canonical examples include `fixtures/browser_evidence/v1/export_flow.json` (settings → export), `checkout_flow.json` (cart → confirm), and `form_validation.json` (single-page errors). Each trace includes navigation, console lines, optional **dom_excerpts** (visible text and/or HTML; may include **aria_role**, **accessibility_name**, **dom_order**), **screenshot** metadata (**capture_scope** viewport | full_page | element, optional **target_selector**, **sequence**, **device_pixel_ratio**, integrity hash), optional **dom_snapshot_ref**, and **extensions** with optional structured **network**, **accessibility**, and **performance** summaries (JSON-serializable).

Summarize the navigation sequence. Explain how excerpts and screenshot scopes support grounding. If **extensions.network** or **extensions.accessibility** are present, relate them to the user-visible narrative without over-claiming. List what you would still capture in a live Playwright trace (optional **Playwright** extra; not required for this task). Note one limitation of fixture-only evidence versus a real browser session.

Remote MCP browser automation is **not** assumed here—treat evidence as committed JSON unless stated otherwise.
