# Tracking: v0.2.3 campaign

**Status:** **Substantive work complete** — implementation and docs for the v0.2.3 themes are **in main**; **release closure** (tag, changelog section, **`docs/releases/v0.2.3.md`**, README/AGENTS “latest shipped”) is the remaining administrative step.

**Roadmap:** **[docs/roadmap/v0.2.3.md](../roadmap/v0.2.3.md)** · **Prior release:** **[docs/releases/v0.2.2.md](../releases/v0.2.2.md)** · **Implementation log:** **[docs/implementation-log.md](../implementation-log.md)**.

---

## Objective (original)

Deliver **v0.2.3** so that campaign **result packs**, **fingerprint-driven** reporting, **readable** outputs, **drift-safe** contracts, and **honest** browser/MCP boundaries are **true in code and docs**—without expanding unrelated surface area.

---

## Completed items

| Area | Evidence |
| --- | --- |
| **Verification surface hardening** | **[docs/workflows/verification.md](../workflows/verification.md)**; **`just ci`** vs **`just validate-artifacts`** vs **`just test`**; README/AGENTS pointers. |
| **Drift checks** | **`tests/test_schema_drift_contracts.py`**; **`just validate-artifacts`**; **[docs/audits/schema-drift-contracts-inventory.md](../audits/schema-drift-contracts-inventory.md)**. |
| **Campaign result-pack refinement** | **`alwm benchmark campaign pack`** / **`pack-check`**; **`campaign_result_pack`**; examples **`examples/campaign_result_packs/`**; **[docs/workflows/benchmark-campaigns.md](../workflows/benchmark-campaigns.md)**. |
| **Fingerprint interpretation** | **`fingerprint_axis_interpretation`** (and related) in analysis JSON; comparative + longitudinal Markdown; aligned with **`group_snapshots_by`** / longitudinal semantics. |
| **Browser reporting realism** | Browser evidence in reports where applicable; fixture-backed paths default in CI; **`docs/architecture/browser.md`** + **[docs/audits/capability-classification.md](../audits/capability-classification.md)**. |
| **Pack comparison** | **`alwm benchmark campaign compare-packs`**; kind **`campaign_result_pack_comparison`**; **`examples/campaign_result_packs/compare_minimal_vs_multi/`**. |
| **Report readability** | Campaign/comparative/semantic Markdown structure passes (see **implementation log** entries). |

---

## Remaining items (release closure)

- Add **`[0.2.3]`** section to **`CHANGELOG.md`** when tagging (fold **`[Unreleased]`** as appropriate).
- Write **`docs/releases/v0.2.3.md`** with narrative + verification commands (mirror roadmap).
- Update **`README.md`** / **`AGENTS.md`** **Latest shipped release** table (and top-line changelog links) to **v0.2.3** after tag. *(Current milestone / in-flight blurbs already reflect v0.2.3 substantive completion.)*
- Optionally open **`docs/roadmap/v0.2.4.md`** (or next milestone) for follow-ups below.

---

## Open questions (carry forward)

- **Deduplication:** Which report blocks may **repeat** (executive vs appendix) vs should be **single-sourced** from shared renderers—ongoing editorial preference.
- **Fingerprint narrative:** How much is **template-generated** vs hand-tuned in templates—tune per release.
- **Redaction policy** for packs shared outside the repo—doc-only vs future CLI enforcement.
- **Campaign-level** metadata for semantic-stability thresholds—documentation-first vs harness knobs later.
- **Stronger codegen** (single source for Schema + Pydantic)—post-milestone architecture decision.

*(Resolved for v0.2.3: dedicated **pack comparison** is **`compare-packs`** + **`pack-compare.json`**.)*

---

## Follow-up work (post–v0.2.3 / next milestone candidates)

- Codegen or single-source schema generation to reduce **classes** of drift.
- Deeper **diff** UX (two **`campaign-result-pack.json`** files beyond current compare output).
- **Zip/tar** conventions for teams needing single-file handoff.
- **Campaign-level** semantic-stability threshold metadata (with tests) if product asks for it.

---

## Recommended merge order (next passes)

Use when landing **follow-up** work after v0.2.3 is tagged (same dependency order as before; narrow PRs):

1. **Contracts** — schemas, Pydantic, **`artifacts.py`**, extend **`test_schema_drift_contracts`** when new kinds or paths appear under **`examples/`**.
2. **Pack tooling** — **`campaign_result_pack`** / **`campaign_result_pack_comparison`** before large Markdown refactors that cite paths.
3. **Reporting** — comparative / semantic / longitudinal consumers **after** analysis JSON shapes are stable.
4. **Readability** — copy/structure passes once metrics are final.
5. **Browser / audits** — **`browser.md`**, **`capability-classification.md`**, fixtures—unless a doc-only fix can merge independently.

**Cross-cutting:** Update **`docs/workflows/benchmark-campaigns.md`**, **`verification.md`**, **`README`**, **`AGENTS`** in the same PR as behavior changes, or immediately after.

Parallel agents: **`docs/workflows/multi-agent-parallel.md`** — serialize **`artifacts.py`**, **`schemas/v1/`**, **`cli.py`**.

---

## Verification commands

Authoritative list: **[docs/roadmap/v0.2.3.md](../roadmap/v0.2.3.md)** — Verification commands.

---

## See also

- **[docs/roadmap/v0.2.0.md](../roadmap/v0.2.0.md)** — fingerprints + campaigns arc.
- **[docs/tracking/campaign-orchestration.md](campaign-orchestration.md)** — orchestration tracking.
- **[docs/workflows/benchmark-campaigns.md](../workflows/benchmark-campaigns.md)** — campaigns, packs, compare.
- **[docs/workflows/verification.md](../workflows/verification.md)** — canonical verification matrix.
