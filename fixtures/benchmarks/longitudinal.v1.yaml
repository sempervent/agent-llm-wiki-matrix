# Definition matching fixtures/longitudinal/paired runs (suite identity; titles may differ per run).
schema_version: 1
id: bench.fixtures.longitudinal.v1
title: Longitudinal fixture (baseline week)
rubric_ref: examples/dataset/rubrics/comparison.v1.json
prompts:
  - id: p-one
    text: "fixture"
variants:
  - id: v-cli
    agent_stack: alwm-cli
    execution_mode: cli
    backend:
      kind: mock
      model: mock-model
taxonomy:
  taxonomy_version: 1
  task_family: documentation
  difficulty: low
  determinism: deterministic_scoring
