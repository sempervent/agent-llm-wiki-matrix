# Deterministic: file-backed checkout_flow fixture (rich excerpts + network/a11y extensions).
schema_version: 1
id: bench.fixtures.browser.checkout.v1
title: Browser file-runner benchmark (checkout fixture)
rubric_ref: examples/dataset/rubrics/browser_realism.v1.json
prompts:
  - id: p-one
    text: "Summarize the checkout trace: cite navigation URLs, dom excerpts, screenshot scopes, and one risk from the network summary."
variants:
  - id: v-checkout
    agent_stack: browser-checkout
    execution_mode: browser_mock
    backend:
      kind: mock
      model: mock-model
    browser:
      runner: file
      fixture_relpath: fixtures/browser_evidence/v1/checkout_flow.json
